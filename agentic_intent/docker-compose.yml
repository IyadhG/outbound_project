version: '3.8'

services:
  intent-agent:
    build: .
    container_name: intent-agent
    volumes:
      - ./data:/app/data  
      - ./.env:/app/.env  
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped